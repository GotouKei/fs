<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CharaAll"
                  android:label="@string/app_name"
                  android:theme="@style/MyTheme"/>
        <activity android:name=".CharaCreate"
                  android:label="@string/app_name"
                  android:theme="@style/MyTheme"/>
        <activity android:name=".CharaConplete"
                  android:label="@string/app_name"
                  android:theme="@style/MyTheme"/>
        <activity android:name=".CharaContent"
                  android:label="@string/app_name"
                  android:theme="@style/MyTheme"/>
        <activity android:name=".PartySort"
                  android:label="@string/app_name"
                  android:theme="@style/MyTheme"/>
        <activity android:name=".BattaleStart"
                  android:label="@string/app_name"
                  android:theme="@style/MyTheme"/>
        <activity android:name=".StrategyChange"
                  android:label="@string/app_name"
                  android:theme="@style/MyTheme"/>
        <activity android:name=".BattaleMain"
                  android:label="@string/app_name"
                  android:theme="@style/MyTheme"/>
        <activity android:name=".BattaleEnd"
                  android:label="@string/app_name"
                  android:theme="@style/MyTheme"/>

    </application>

</manifest>